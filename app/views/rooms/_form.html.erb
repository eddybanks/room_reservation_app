<%= form_for(@room) do |f| %>
  <% if @room.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@room.errors.count, "error") %> prohibited this room from being saved:</h2>

      <ul>
      <% @room.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :building %><br>
    <%= f.select :building_id, Building.all.map{|b| [b.name, b.id]}, include_blank: 'Select a Building' %>
  </div>
  <div class="field">
    <%= f.label :capacity %><br>
    <%= f.select :capacity, 1..100 %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
